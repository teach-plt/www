#!/usr/bin/env bash

# This is a simple wrapper script for a Jasmin jar file. Add this script to your
# $PATH, with the Jasmin jar in the same directory.

# Get script directory: https://stackoverflow.com/a/246128
#
# "readlink -f" is cleaner, but does not work on Mac without e.g. the brew
# package coreutils. And if the user has brew, they might as well install Jasmin
# directly...
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

java -jar "$DIR/jasmin.jar" "$@"
