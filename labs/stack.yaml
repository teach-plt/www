# Any snapshot for GHC >= 8.4 should work, we pick the most recent final LTS:
# (recent as of 2026-09-03)
resolver: lts-23.28

# See https://www.stackage.org/ "Latest releases per GHC version"
# for alternatives.
